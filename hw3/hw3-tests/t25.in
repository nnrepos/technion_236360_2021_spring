//tests TypeRules #11 & 12, string expression can be used only with print function.
void main() {
    print("this is a string");
    byte c = 15b;
    int x = ((1+5) * (2+3) + c)/3;
    printi(x);
    printi(c);
}
