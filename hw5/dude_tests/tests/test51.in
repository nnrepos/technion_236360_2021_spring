void maxTestsTestBreak() {
	int x;
	while(true) {
		if (x == 10) break;
		x = x + 1;
		printi(x);
	}
	print("Success is not final, failure is not fatal: it is the courage to continue that counts. – Winston Churchill");	
}

void main() {
     maxTestsTestBreak();
}
