void main()
{
    int x = 0;
    int y = 0;
    while (x < 20) {
        switch(y) {
            case 0: {
                x = x + 1;
                if (x >= 5) {
                    y = y + 1;
                }
                break;
            } 
            case 1: {
                int z = 0;
                while(z < 5) {
                    x = x + z;
                    break;
                }
                if (x >= 10) {
                    y = y + 1;
                }
                break;
            }
            default:
                print("default");
                x = x + 3;
        }
        x = x + 1;
        printi(x);
    }
}

