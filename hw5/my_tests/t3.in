int retFromSwitch(int x){
    switch(x){
        case 4: printi(4);
        case 5: return(5);
        default: return(6); break;
    }

}

void main(){
    printi(retFromSwitch(4));
    printi(retFromSwitch(5));
    printi(retFromSwitch(6));
}